<?xml version="1.0" encoding="UTF-8"?>
<lint>
    <!-- Suppress because Picasso's NotificationAction triggers this via static analysis
         but the app never actually posts notifications through Picasso. -->
    <issue id="NotificationPermission" severity="warning" />
</lint>
