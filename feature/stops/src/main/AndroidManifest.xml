<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Declared here as well as in app and common:utils so the connectivity APIs used by the
         lines browser's offline handling are permission-visible at this module's level. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
</manifest>
