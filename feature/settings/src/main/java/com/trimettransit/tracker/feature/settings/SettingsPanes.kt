package com.trimettransit.tracker.feature.settings

/**
 * Settings panes, one category per file in this package (e.g. [AppearancePane],
 * [ColoursPane]). This file remains as the original home of the split; it carries
 * no composables itself.
 */
